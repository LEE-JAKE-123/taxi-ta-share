# P0 정산 경로 정정

관련 요구사항: `FR-35~40`, `FR-50`, `FR-52~54`, `TR-01~03`

## 현재 구현의 경계 (DEC-011·DEC-014 적용 전)

- 실제 요금 제출은 `IN_PROGRESS → SETTLEMENT_PENDING` 전이와 확정 예치 인원 스냅샷만 만든다. 이 시점에는 포인트 원장, 잔액, 참여 완료 상태를 변경하지 않는다.
- 새 정산은 `ESCROW_CONFIRMED` 예치 cohort를 사용한다. `NO_SHOW` 참여자도 분모와 정산 원장에 포함하며, 새 `BOARDED` 정산은 허용하지 않는다.
- 참여자는 실제 요금을 확인하거나 이의제기할 수 있다. 열린 이의제기가 있으면 최종 정산은 차단된다.
- 확인과 이의제기는 참여자별로 하나만 선택할 수 있다. 같은 정산 행 잠금과 DB 트리거가 동시 요청도 직렬화하며, 이미 기록된 확인의 재시도는 확인 기한 뒤에도 무해하게 처리한다.
- 최종 정산은 전원 확인 또는 서버가 판단한 확인 기한 경과 뒤 방장 요청으로만 실행한다. 원장·잔액·참여자·모집·정산 상태는 하나의 직렬화 트랜잭션에서 변경한다.

## 데이터베이스 변경

`0014_confirmed_cohort_settlement.sql`은 과거 `BOARDED` 정산을 수정하지 않는다. 기존 `ESCROW_CONFIRMED` 정산에는 누락된 cohort 스냅샷만 보강하고, 이후 원장 insert는 `SETTLEMENT_PENDING`과 `PENDING_CONFIRMATION` 상태 및 확정 cohort 금액을 만족해야 한다.

## 확정된 동의·이의제기·외상 정책

- 실제 요금 등록 후 전원 동의 또는 10분 동의 기한 만료 시 잠정 정산한다.
- 이의제기는 실제 요금 등록 후 24시간 안에 가능하다. 잠정 정산 뒤 이의가
  접수되거나 처리돼도 과거 원장을 수정하지 않고 보정 원장으로 처리한다.
- 잔액이 부족한 참여자는 예치금과 사용 가능 포인트를 먼저 정산하고, 남은
  부족분만 개인 외상으로 기록한다. 다른 참여자의 정산은 지연하지 않는다.
- 미상환 외상 사용자는 정상 이용을 할 수 없다. 다만 `OPEN` 이의가 연결된
  외상은 예외로 하며, 미해결 이의가 3건 이상인 사용자는 3건 미만이 될 때까지
  정상 이용을 차단하고 이유와 해소 방법을 안내한다.
- 관리자 포인트 지급은 가장 오래된 외상부터 상환하고, 남은 금액만 일반 포인트로
  지급한다.

## 남은 열린 결정

- 취소 수수료·환불, 노쇼 추가 제재

## 확정된 나머지 포인트 배분 정책

실제 총요금 `A`를 확정 예치 인원 `N`으로 정산할 때 전원에게
`ceil(A/N)`을 차감하지 않는다. 방장을 첫 순서로 두고, 나머지 참여자는
서버가 기록한 승인 시각 오름차순(동률은 사용자 ID 오름차순)으로 정렬한다.
앞의 `N - (A mod N)`명은 `floor(A/N)`을, 나머지는 `floor(A/N) + 1`을
부담한다. 따라서 개인별 부담액은 1P 이내로만 다르고 총합은 항상 `A`와
같다.

예를 들어 3명이 10,000P를 정산하고 방장 다음으로 A, B가 확정됐다면
방장 3,333P, A 3,333P, B 3,334P를 부담한다. 참여자별 확정 부담액은
정산 스냅샷에 보존해 재시도·분쟁 처리·감사에서도 같은 결과를 사용해야 한다.

정책 v2 마이그레이션 `0022`~`0028`과 서버 트랜잭션은 잠정 정산, 24시간 이의,
외상 및 보정 원장을 append-only 단위로 도입 중이다. 기존 완료 정산과 원장은
수정하지 않으며, 배포 전에는 격리 DB에서 마이그레이션·동시성·브라우저 E2E를
검증해야 한다.

## 기존 구현의 이의제기 흐름 (교체 대상)

- 이의제기는 참여자가 확인 기한 안에 철회할 수 있으며, 철회 이력은 삭제하지 않는다.
- 활성 관리자만 이의를 기각하거나 수정 요금 재제출을 요청할 수 있다. 기각 후에는 기존 실제 요금의 확인·정산을 재개한다.
- 수정 요금 재제출 요청은 아직 원장·잔액이 바뀌지 않은 정산 제안만 무효화한다. 방장은 새 실제 요금을 제출하고, 이전 확인은 지운 뒤 새 확인 기한을 시작한다.
- 하나의 이동에 열린 이의가 여러 건이면 모든 이의를 처리하기 전에는 수정 요금을 요청할 수 없다.

## 실제 요금 입력자 MVP 정책

- 방장은 출발 전 예치가 완료된 확정 참여자 한 명을 실제 요금 입력자로 지정할 수 있으며, 방장은 언제나 입력 권한을 유지한다.
- 지정 변경은 `CONFIRMED` 상태에서만 가능하고, 출발 뒤에는 지정 당시 권한만 사용한다.
- 수정 요금 재제출은 이의 해결 흐름의 방장 전용 권한으로 유지한다.

## 이번 검증

- `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test`를 실행했으며, Vitest는 15개 파일·122개 테스트를 통과했다.
- `db:verify`는 검증용 Neon 연결 정보가 없어 실행하지 못했다. 마이그레이션 적용 뒤에는 같은 명령으로 0014의 스냅샷·트리거 검증을 실행해야 한다.
- 프로덕션 빌드는 외부 Google 글꼴 다운로드가 제한된 실행 환경에서 실패한다. 이 실패는 이번 TypeScript/정산 변경과 별개이며, 네트워크가 가능한 CI에서 재확인이 필요하다.
